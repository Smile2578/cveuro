
export const metadata = {
    title: 'Générateur de CV',
    description: 'Générez votre CV en quelques clics',
  };
  
  export default function CVGenLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  